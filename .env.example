# Supabase — same project as the NAQD statutory tracker.
# Copy to .env and fill in. Find these in Supabase Dashboard -> Project Settings -> API.
# Leaving them blank runs the app in local DEMO mode (sample data, no backend).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
